{
  "name": "pi-tencentdb-memory",
  "version": "0.1.0",
  "description": "Pi extension for TencentDB Agent Memory — self-hosted team memory across machines with zero runtime dependencies",
  "keywords": ["pi-package", "tencentdb", "agent-memory", "pi-extension", "self-hosted"],
  "type": "module",
  "license": "GPL-3.0-only",
  "pi": {
    "extensions": ["./extensions"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@types/node": "^22.10.0",
    "typebox": "1.3.7",
    "typescript": "^5.7.0"
  }
}
